#pragma once

#include <iostream>
#include <string>
#include <cstdlib>
#include <sstream>
#include <fstream>

using namespace std;

void Compress(const char *in, const char *out);
void Decompress(const char *in, const char *out);